Para optener un elemento del DOM lo seleccionamos desde la DevTool de navegador 
 despues vamos a consola y copiamos $0 esto nos traera ese elemento

getElementById() = para llamar a un id.
querySelector() = sirve para llamar a un elemento tanto #id , como clases.
quetySelectorAll() = traerá a todos los elementos del documento que coincidan con los selectores especificados entre los paréntesis.
getElementsByTagName() = sirve para llamar solo elementos HTML.
getElementsByclassName() = sirve para llamar a las clases del documento entero.